#Just an alias command to simplify running commands
alias observability="docker-compose -f docker-compose.yml -f docker-compose.observability.yml -f observability-network.yml"
alias mesh="docker-compose -f docker-compose.yml -f docker-compose.observability.yml -f docker-compose.service-mesh.yml -f mesh-network.yml"
